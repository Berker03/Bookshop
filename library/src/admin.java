public class admin extends user {

    public admin(String userId, String userName, String surname, Address address, Double balance, String role) {
        super(userId, userName, surname, address, balance, role);
    }
}